# Notes:

- **Const**: It is good to used all upper case variable to define a "constant".
- Array in C-language don't have the length feature like python. So, We have to declare Length in order to use it.
- If we write **string s = "HI!"** in (cs50.dev) it will act like an array. |H|I|!|.
- /0 is a special character consist of 8 "0". Which is called "Nul".
- int main(void) or int main(int argc, string argv[]) can be said
